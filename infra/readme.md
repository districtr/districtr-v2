# Districtr Infrastructure

Quickstart:

Add rellevant AWS credentials to your environment. Then: 

```
npm install
npm run pulumi:up
```

Pulumi logs are stored in mggg AWS bucket name `districtr-pulumi`