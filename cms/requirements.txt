# Wagtail LTS line
wagtail>=7.0,<7.1
Django>=5.2,<5.3

# Auth issuer (RS256 JWTs verified by the FastAPI backend)
djangorestframework>=3.15
djangorestframework-simplejwt[crypto]>=5.4

# Localization (en/es/zh/vi/ht/pt content trees)
wagtail-localize>=1.11

# Media on S3/Cloudflare R2
django-storages[s3]>=1.14

# Service-to-service calls to the FastAPI backend (already a transitive
# Wagtail dependency; pinned because datastore.services imports it directly)
requests>=2.31

# Transactional email via Resend
django-anymail[resend]>=12.0

# Database
psycopg[binary]>=3.1

# Serving
gunicorn>=23.0
whitenoise>=6.7
